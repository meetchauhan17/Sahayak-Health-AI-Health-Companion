# CORS — allow the local frontend dev server and the deployed Vercel frontend.
# Read allowed origins from env so the value can be set per-environment.
FRONTEND_URL=http://localhost:3000

# You MUST set these in your deployment environment (e.g. Render dashboard):
GROQ_API_KEY=your_groq_api_key_here
